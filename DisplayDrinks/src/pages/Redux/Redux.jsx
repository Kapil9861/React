//  npm install @reduxjs/toolkit
//npm install redux-redux
// We can do global state management in redux where we have
// slice where args and reducer are based and can be added to Store we can
// also make multiple slice

//First and simple app
